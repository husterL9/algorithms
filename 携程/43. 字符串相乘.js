/**
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
var multiply = function (num1, num2) {
  /**
   *
   * @param {string} num
   * @param {string} digit
   * @return {string}
   */
  function digitMultiply(num, digit) {
    let len = num.length
    let carry=
  }
}
